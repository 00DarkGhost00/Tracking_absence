<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>University Absence Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f8f9fa; }
        .sidebar {
            height: 100vh;
            width: 250px;
            position: fixed;
            top: 0;
            left: 0;
            background-color: #002147; /* Navy Blue */
            color: white;
            padding-top: 20px;
        }
        .sidebar a {
            padding: 15px 25px;
            text-decoration: none;
            font-size: 1.1rem;
            color: #bdc3c7;
            display: block;
            transition: 0.3s;
        }
        .sidebar a:hover { color: white; background-color: #003366; }
        .sidebar .active { background-color: #0d6efd; color: white; }
        .main-content { margin-left: 250px; padding: 40px; }
        .university-logo { width: 120px; display: block; margin: 0 auto 20px; }
        .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); border-radius: 10px; }
        .btn-primary { background-color: #002147; border: none; }
        .btn-primary:hover { background-color: #003366; }
    </style>
</head>
<body>

<div class="sidebar">
    <img src="{{ url_for('static', filename='images/logo.png') }}" class="university-logo" alt="Logo">
    <h5 class="text-center mb-4">Portail Absence</h5>
    <hr>
    <a href="{{ url_for('index') }}" class="{{ 'active' if request.endpoint == 'index' }}">📝 Saisie Quotidienne</a>
    <a href="{{ url_for('dashboard') }}" class="{{ 'active' if request.endpoint == 'dashboard' }}">📊 Statistiques</a>
    <a href="{{ url_for('manage_data') }}" class="{{ 'active' if request.endpoint == 'manage_data' }}">⚙️ Gérer les Données</a>
</div>

<div class="main-content">
    {% block content %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>